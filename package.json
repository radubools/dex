{
  "name": "dex-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": ["web"],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "dev:ui": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web"
  }
}
